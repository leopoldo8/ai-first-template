{
  "$schema": "https://json-schema.org/schema",
  "id": "bounded-context",
  "title": "Bounded Context Generator",
  "description": "Creates a new bounded context with Clean Architecture layers",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the bounded context (e.g., orders, payments)",
      "x-prompt": "What is the name of the bounded context?"
    }
  },
  "required": ["name"]
}
