// NestJS application entry point
// Replace with actual NestJS bootstrap when dependencies are installed
//
// import { NestFactory } from '@nestjs/core'
// import { AppModule } from './app.module.js'
//
// async function bootstrap(): Promise<void> {
//   const app = await NestFactory.create(AppModule)
//   app.enableCors()
//   await app.listen(3000)
// }
//
// bootstrap()

export async function bootstrap(): Promise<void> {
  // Placeholder until NestJS dependencies are installed
  // See docs/guides/getting-started.md for setup instructions
  console.log('API server placeholder — install NestJS dependencies to run')
}

bootstrap()
