# Deployment

## Prerequisites

- All tests passing: `npm exec nx run-many -t test`
- All lints passing: `npm exec nx run-many -t lint`
- Build succeeds: `npm exec nx run-many -t build`

## Process

(To be defined by template users based on their deployment target)
